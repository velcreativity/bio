# 품질심사문서_2026 레이아웃 수정 기록 (2026-07-14)

입력: 사용자 업로드 `품질심사문서_2026.hwpx` (GPT 재구축본, 30섹션 = 폼당 1섹션, 173,965B).
두 번째 업로드 파일은 MD5 동일(`09aa591b…`) — 같은 파일이 두 번 올라온 것 확인.
출력: `품질심사문서_2026_수정본.hwpx` (174,802B) — 사용자에게 직접 전달 (R6: 바이너리 git 미반영).

## 진단 결과 (수정 전)

| 항목 | 상태 |
|---|---|
| 머리말/꼬리말 | 전 섹션 1+1 존재 (구조 자체는 정상) |
| 머리말 밴드 초과 | **17개 섹션** — 밴드 6116 vs 머리말 표 최대 12692 (본문 겹침 원인) |
| 상단 여백 | top=11000 (38.8mm) — 도너(2024) 관례는 제목이 종이 상단 ~11mm에서 시작 |
| 꼬리말 밴드 | 1200 — 꼬리말 2줄(구분선+라벨, ~3300) 수용 불가 → 표 하단에 붙는 현상 |
| 페이지 폭 초과 표 | **2개** (sec28: 68875, sec29: 68592 > 가용 53856 — 종이 폭 59528보다도 넓음) |
| 페이지 높이 초과 표 | 20개 (행높이 합 최대 141308 = 본문 2.3쪽) |
| 셀 내용 과밀 | 26개 표에서 압력 >1.05 (최대 6.98 — 내용량이 셀 용량의 7배) |
| 표 개체속성 | 전부 treatAsChar="1" (글자처럼 취급) — 사용자 지정 조합 미적용 |
| dangling 스타일 참조 | 0 (크래시 위험 없음) |
| linesegarray 캐시 | 0 |
| 꼬리말 PUA 문자 (U+F081A ×92/섹션) | **정상** — 원본 2024 도너에도 2,615개 존재하는 한컴 구분선 문자. 보존. |

## 적용한 수정 (우선순위 = 사용자 지시)

1. **페이지 맞춤**: 폭 초과 표 2개(sec28/29)를 가용폭 53856으로 비례 축소
   (행별 누적-반올림으로 열 경계 정합 유지, 행 합계 오차 0).
2. **폰트 축소 ≤20%**: 과밀 셀(압력>1.15)의 런을 축소 charPr 클론으로 재매핑.
   축소율 s=max(0.8, 1/√압력), 50단위 반올림, 하한 6pt. 클론 26종 추가, itemCnt 112→138.
3. **글자처럼 취급 해제 + 쪽경계 셀단위 나눔** (잔여 변형 위험 표 27개):
   `treatAsChar 1→0` + `pageBreak="CELL"` + `repeatHeader="1"`.
   기존 `flowWithText="1"`(쪽 영역 안쪽 제한)·`allowOverlap="0"`(겹침 금지)·
   `textWrap="TOP_AND_BOTTOM"`은 이미 있었으므로 유지 — 사용자 지정 3종 조합 완성.
   1쪽 내 수용되는 표 16개는 인라인 유지 (무변경).
4. **머리말 밴드**: 섹션별 머리말 표 높이+400으로 재설정 (5236~13092) — 밴드 초과 0.
5. **여백 도너 관례 정렬**: top 11000→2500 (제목이 최상단 근처에서 시작),
   footer 1200→3400 (구분선+라벨 2줄 수용), bottom 4500 유지.

## 게이트 (전 항목 PASS)

- XML 정형성 (섹션 30 + header + META-INF) / dangling 참조 charPr·paraPr·style·borderFill 0
- itemCnt 선언 = 실제 개수 / 폭 초과 표 0 / 머리말 밴드 초과 0
- R-PKG-1 archive-clone 재포장: 엔트리 40개 순서·압축타입 보존, mimetype 선두 STORED,
  디렉토리 엔트리 0, CRC OK

## 잔여 확인 (한컴 렌더 필요 — 클라우드에서 검증 불가)

1. 한컴에서 열림/크래시 여부, CELL 분할 시 페이지 간 표 흐름 육안 확인.
2. 축소 폰트 셀(26곳)의 가독성 — 압력 산식은 휴리스틱이므로 과축소 시 해당 셀만 되돌릴 것.
3. 환경심사문서_2026도 동일 증상이라면 같은 파이프라인 적용 가능 — 파일 업로드 필요.
